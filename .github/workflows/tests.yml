name: tests
on:
  push:
    branches-ignore:
      - 'private/**'
  pull_request:
    branches-ignore:
      - 'private/**'
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.0
      - name: Setup Go
        uses: actions/setup-go@v6.5.0
        with:
          go-version: 1.25.x
      - name: Run make check
        run: |
          tools/ci-build.sh
